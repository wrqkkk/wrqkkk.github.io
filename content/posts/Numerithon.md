+++
date = '2026-05-30T20:20:00+08:00'
draft = false
title = '20-hour Numerithon'
isCJKLanguage = true
math = true
+++


Inspired by marathon/hackathon.


实在是没时间了，快速搞定final。




# Stage1 Concept Map（预估30min，实际40min）


搞清楚这门课在干什么，讨论的对象是什么，聚焦什么问题，用什么视角切入，提出了哪些解决方法。


理解五个Keywords：误差、插值、逼近、积分、求根。

## 误差

首先是误差，计算数学和数学相比起来，重心上的区别就在于——我们讨论数学可以随便讨论某个值，例如根号二，实数连续统告诉我们，它有一个绝对确定的、唯一的无限不循环小数在表示它，我们只需要使用它而不需要具体算出来它是多少；但在实际应用中，具体到小数点后几位对我们来说很重要，尺子上的刻度本身有宽度，1cm和1cm或许不是同一个1cm；钢笔勾出的线有宽度，我们画出来的图也有误差，砝码会生锈，天平部件会松动......这种理论值和实际值的不同我们称之为误差（error），既然误差难以避免，那么我们的关注点就转向讨论：究竟怎样才是可接受的误差？

于是我们引入了绝对误差的概念，很多时候理论值我们并不知道，于是有了绝对误差限——还不够——如果是买水果，出入十来克无所谓，可如果是买金子，哪怕1克都会带来几百的损失；造碗和造火箭零件的精度要求更不是一个量级，怎么办？——统一“度量衡”，引入相对误差，先去掉数量级的差异，再根据具体任务的要求确定，要把相对误差控制在什么范围内能接受，同样的，引入相对误差限。


什么是有效数字？有效数字与相对误差的关系


数值运算的误差估计方法
- 微分运算估计绝对误差、相对误差
- 泰勒展开估计

避免误差危害构造稳定算法的方法list




## 插值

啥是插值？我有n个点（**插值节点**），要尽可能还原生成这些点的“机制”，也就是原来的函数，我们要利用这n条信息；另外，为了方便让我们“想”出来的函数（即**插值函数**）经过这n个点，也为了方便计算（预测之后or之中没有出现的的maybe？或者研究这个自变量对因变量的影响？），我们的**插值函数**一般是比较special的，比如多项式，多项式函数空间有一些非常好的性质（高代内容），或许这是为什么我们用它。


插值多项式是唯一的（by之前所说的$\mathscr{P}_n$空间的大好性质），这非常好，虽然不知道为什么好，但数学喜欢存在性和唯一性的讨论，有时间可以进一步研究一下。



有一些不同的构造多项式插值函数的方法，比如说lagrange插值多项式、newton插值多项式，还有hermite插值多项式，另外还有legendre多项式，不知道这里有没有涉及，在学pde的时候我们有讨论过这个多项式，可以回头看一看。


进一步地，我们要讨论插值在实际应用中可能会出现的问题——Runge现象，它是什么？什么情况下会产生？如何避免？：
- 是什么：
- 产生原因：等距节点**高次**插值会产生Runge现象
- 如何避免：不要高次数，分段低次插值
  - 分段线性插值基函数
- 分段线性插值多项式以及**余项**



## 逼近
（卧槽到这里已经花了1h了，加油吧姐）


啥是逼近？刚刚我们不是讨论了插值嘛，既然真实的函数我们不知道，找了一个插值函数作为替代品来进行计算，那我们当然要关心这个“替代品”是否足够“好用”，用某种”度量“来衡量理论值和实际值之间的区别——所以我们要讨论什么？一个类似”误差“的东西，这个误差到这里不是一个值了，而是两个 $\aleph_1$ 的点集之间的差距，一个个算当然是不现实的（不过可以用积分这个工具来做“一个个算并加起来“的工作），就像误差和相对误差有时候并不可得一样，我们会引入一个类似于“限”的东西——函数空间的范数——来讨论我们找的函数相对真实函数的误差。包括：
- 一致逼近范数
- 平方逼近范数




1h10min

去汇报了



